<UserControl x:Class="FicheroNacionalPip.Presentation.Views.RightMenu.LoginWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"  xmlns:rightMenu="clr-namespace:FicheroNacionalPip.Presentation.ViewModels.RightMenu"
             d:DataContext="{d:DesignInstance Type=rightMenu:LoginViewModel, IsDesignTimeCreatable=True}">

    <Grid Cursor="Hand" Background="Transparent" >
        <Border BorderBrush="Black" Background="Beige" HorizontalAlignment="Stretch" Padding="4" BorderThickness="3" CornerRadius="10" Margin="0">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Opacity="80" ShadowDepth="16"></DropShadowEffect>
            </Border.Effect>
            <Grid>
                <TextBlock Text="{Binding MyTitle}" VerticalAlignment="Top" Height="60" FontWeight="Bold" FontSize="36" Margin="5" Foreground="Black" />
            </Grid>
        </Border>
    </Grid>

</UserControl>
